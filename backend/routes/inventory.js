const express = require('express');const router = express.Router();const inventoryController = require('../controllers/inventoryController');const { verifyToken, requireRole } = require('../middleware/auth');router.use(verifyToken);router.get('/logs', requireRole('shop', 'admin'), inventoryController.getInventoryLogs);router.post('/adjustment', requireRole('shop', 'admin'), inventoryController.addInventoryAdjustment);router.get('/alerts', requireRole('shop', 'admin'), inventoryController.getStockAlerts);router.get('/summary', requireRole('shop', 'admin'), inventoryController.getInventorySummary);router.get('/report', requireRole('shop', 'admin'), inventoryController.exportInventoryReport);module.exports = router;